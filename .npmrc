# Expo y las librerias del ecosistema declaran rangos de React distintos entre
# si. La resolucion estricta de peers bloquea la instalacion sin que haya un
# problema real. Fijarlo aqui evita que cada persona (y el CI) recuerde la
# bandera, y hace que `npm ci` funcione sin argumentos extra.
legacy-peer-deps=true
